{
  "todos": [
    "Run scripts\\validate-installed-startup.ps1 against the installed app and confirm no new WER/Application Error events for OnlyWinget.exe.",
    "Manually validate Italian and English visible navigation, command, source-management, status, empty, loading, error, and operation result labels.",
    "Manually validate the redesigned WinUI shell at minimum, default, and maximized window sizes in Italian and English.",
    "Manually validate preset, search, update, source-management, activity, cancellation, and select-all flows on a Windows host with winget installed.",
    "Run winget smoke tests with .\\scripts\\test.ps1 -RunWingetSmoke on a machine with winget installed and configured.",
    "Manually validate winget source update, add, remove, and reset flows with safe test sources and confirmations.",
    "Resolve dirty tracked files before release validation.",
    "Run elevated package lifecycle validation on a clean or dedicated Windows host, including install, uninstall, upgrade, winget source update, and startup smoke validation.",
    "Run the hosted GitHub Actions build-gate workflow for the exact release commit before tagging."
  ]
}

{
  "todos": [
    "Fix scripts\\check.ps1 so -NonInteractive is forwarded to every nested repository script.",
    "Manually validate the redesigned WinUI shell at minimum, default, and maximized window sizes in Italian and English.",
    "Manually validate all seven pages at compact, intermediate, and wide widths in Italian and English and in light and dark themes, including empty, loading, error, mixed-selection, disabled-command, and operation-result states, with no overlap, truncation, or page-level horizontal scrolling.",
    "Manually validate preset, search, update, safe source refresh/update/add/remove/reset with confirmations, activity, cancellation, and select-all flows on a Windows host with winget installed.",
    "Manually validate capability-gated Windows Update scan, select-all, selected install, cancellation, and reboot-required reporting on Windows 10 1809 or newer.",
    "Run winget smoke tests with .\\scripts\\test.ps1 -RunWingetSmoke on a machine with winget installed and configured.",
    "Manually validate the setup wizard layout and OnlyWinget visibility in Programs and Features after installing the generated bundle.",
    "Run scripts\\validate-installed-startup.ps1 against the installed app and confirm no new WER/Application Error events for OnlyWinget.exe.",
    "Run elevated package lifecycle validation on a clean or dedicated Windows host, including install, uninstall, upgrade, winget source update, and startup smoke validation.",
    "Run the hosted GitHub Actions build-gate workflow for the exact release commit before tagging."
  ]
}

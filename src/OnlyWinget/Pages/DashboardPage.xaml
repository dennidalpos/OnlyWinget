<Page
    x:Class="OnlyWinget.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="28" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="2">
            <TextBlock x:Name="TitleText" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Name="SummaryText" Style="{StaticResource SubtitleTextBlockStyle}" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Padding="14" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="6">
                <StackPanel>
                    <TextBlock x:Name="WingetValueText" Style="{StaticResource MetricValueTextBlockStyle}" />
                    <TextBlock x:Name="WingetLabelText" Style="{StaticResource MetricLabelTextBlockStyle}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Padding="14" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="6">
                <StackPanel>
                    <TextBlock x:Name="PresetValueText" Style="{StaticResource MetricValueTextBlockStyle}" />
                    <TextBlock x:Name="PresetLabelText" Style="{StaticResource MetricLabelTextBlockStyle}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Padding="14" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="6">
                <StackPanel>
                    <TextBlock x:Name="SearchValueText" Style="{StaticResource MetricValueTextBlockStyle}" />
                    <TextBlock x:Name="SearchLabelText" Style="{StaticResource MetricLabelTextBlockStyle}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Padding="14" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="6">
                <StackPanel>
                    <TextBlock x:Name="UpdateValueText" Style="{StaticResource MetricValueTextBlockStyle}" />
                    <TextBlock x:Name="UpdateLabelText" Style="{StaticResource MetricLabelTextBlockStyle}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="4" Padding="14" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="6">
                <StackPanel>
                    <TextBlock x:Name="SourceValueText" Style="{StaticResource MetricValueTextBlockStyle}" />
                    <TextBlock x:Name="SourceLabelText" Style="{StaticResource MetricLabelTextBlockStyle}" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="RecentActivityText" FontWeight="SemiBold" />
            <ListView x:Name="ActivityList" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,8" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Severity}" Style="{StaticResource RowSecondaryTextBlockStyle}" />
                            <TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <TextBlock x:Name="StatusText" Grid.Row="3" TextWrapping="Wrap" />
    </Grid>
</Page>

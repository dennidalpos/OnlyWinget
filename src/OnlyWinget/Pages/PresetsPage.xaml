<Page
    x:Class="OnlyWinget.Pages.PresetsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Presets" Style="{StaticResource TitleTextBlockStyle}" />
        <CommandBar Grid.Row="1" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="Add" />
            <AppBarButton Icon="Edit" Label="Rename" />
            <AppBarButton Icon="Delete" Label="Remove" />
            <AppBarButton Icon="OpenFile" Label="Import" />
            <AppBarButton Icon="Save" Label="Export" />
            <AppBarButton Icon="Play" Label="Apply" />
        </CommandBar>
        <ListView Grid.Row="2">
            <ListView.Header>
                <TextBlock Text="Preset packages" />
            </ListView.Header>
        </ListView>
    </Grid>
</Page>

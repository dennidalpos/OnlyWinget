<UserControl
    x:Class="OnlyWinget.DesignSystem.Controls.PageScaffold"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="Root" Padding="28,24,28,28" RowSpacing="16">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Compact">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Root.Padding" Value="16,16,16,20" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Medium">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource PageMediumBreakpoint}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Root.Padding" Value="24,20,24,24" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource PageWideBreakpoint}" />
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid MaxWidth="{StaticResource PageMaxWidth}" HorizontalAlignment="Stretch" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Text="{x:Bind Title, Mode=OneWay}" Style="{StaticResource PageTitleTextBlockStyle}" />
                <TextBlock Text="{x:Bind Subtitle, Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}" />
            </StackPanel>
            <ContentPresenter Grid.Column="1" Content="{x:Bind Commands, Mode=OneWay}" VerticalAlignment="Bottom" />
        </Grid>
        <ContentPresenter Grid.Row="1" Content="{x:Bind State, Mode=OneWay}" MaxWidth="{StaticResource PageMaxWidth}" HorizontalAlignment="Stretch" />
        <ContentPresenter Grid.Row="2" Content="{x:Bind Body, Mode=OneWay}" MaxWidth="{StaticResource PageMaxWidth}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <ContentPresenter Grid.Row="3" Content="{x:Bind Footer, Mode=OneWay}" MaxWidth="{StaticResource PageMaxWidth}" HorizontalAlignment="Stretch" />
    </Grid>
</UserControl>
